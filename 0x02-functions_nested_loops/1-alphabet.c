#include "main.h"

/**
 * print_alphabet - prints the alphabet im lowercase
 *
 * Return: 0(success)
 */

void print_alphabet(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar('a');

	_putchar(10);
	return (0);
}
