#include "main.h"

/**
 * print_alphabet - prints the alphabet im lowercase
 *
 * Return: returns nothing
 */

void print_alphabet(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);

	_putchar(10);
}
