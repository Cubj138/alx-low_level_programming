#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 *@c: parameter
 *
 * Return: returns absolute value
 */

int _abs(int c)
{
	if (c < 0)
	{
		return (-c);
	}
	else
	{
		return (c);
	}
	return (0);
}
